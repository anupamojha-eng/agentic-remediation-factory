# ⚠️  INTENTIONALLY VULNERABLE — demo target for Sentinel autonomous remediation
#
# CVE surface spans 5 GHSA advisories across 4 packages:
#
#   GHSA-8q59-q68h-6hv4  PyYAML < 6.0          Arbitrary code execution via yaml.load()
#   GHSA-6q5r-27gj-jm84  PyYAML < 5.4           RCE in full_load (pre-6 series)
#   GHSA-v8gr-m533-ghj9  cryptography < 39.0.1  RSA decryption oracle (Bleichenbacher)
#   GHSA-j8r2-6x86-q33q  requests < 2.31.0      Auth headers leaked on cross-origin redirect
#   GHSA-q2q7-5pp4-w6pg  urllib3 < 1.26.5       Header injection via CRLF in request target
#
# Sentinel must:
#   1. Detect all 5 GHSAs via OSV transitive scan
#   2. Upgrade all 4 packages to safe minimum versions
#   3. Patch app/config.py:  yaml.load() → yaml.safe_load()
#   4. Patch app/cache.py:   pickle.loads() — replace or guard with serialization alternative
#   5. Verify: pip3 install -r requirements.txt && python3 -m pytest -q

PyYAML==5.3.1
cryptography==3.3.2
requests==2.27.0
urllib3==1.24.1
Pillow==8.4.0
Flask==2.0.3
Werkzeug==2.0.3
